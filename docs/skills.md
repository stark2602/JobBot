# ingest_jobs — fetch Greenhouse/Lever/Workday public APIs into JobPosting[]
# match_jobs — dedupe + exclude + score (llm|heuristic)
# export_csv — append CSV + SQLite hashes
# send_digest — one Outlook HTML email
